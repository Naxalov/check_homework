def is_even(num: int) -> bool:
    """A function that returns True if the number is even and False if the number is odd.

    Args:
        num (int): The number to be checked.

    Returns:
        bool: True if the number is even and False if the number is odd.
    """
    return num % 2 == 0